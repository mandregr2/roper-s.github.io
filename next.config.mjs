/** @type {import('next').NextConfig} */

const nextConfig = {
    output: 'export',
    trailingSlash: true,
    basePath: '/roper-s.com', 
    assetPrefix: '/roper-s.com',
  }
  
  export default nextConfig;